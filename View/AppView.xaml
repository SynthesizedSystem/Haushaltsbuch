<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Haushaltsbuch.View.AppView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Haushaltsbuch.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:views="using:Haushaltsbuch.View"
    mc:Ignorable="d">

    <StackPanel Padding="25">

        <!-- Window header -->
        <controls:DockPanel Background="#2c3e50" Padding="15" Height="65" Margin="0 10 0 0">
            <TextBlock Text="💰Haushaltsbuch" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="24" controls:DockPanel.Dock="Left"  FontWeight="Bold" />
            <!--<TextBlock Text="Balance: 2400$" VerticalAlignment="Center" HorizontalTextAlignment="Right" controls:DockPanel.Dock="Right" />-->
        </controls:DockPanel>


        <controls:UniformGrid Columns="2" Rows="2" Margin="0 15 0 0"  RowSpacing="15">

            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource BorderGroupStyle}">
                <views:NewTransactionView />
            </Border>

            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource BorderGroupStyle}">
                <views:LastTransactionsView />
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource BorderGroupStyle}">
                <views:CategoriesView />
            </Border>

            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource BorderGroupStyle}">
                <views:MonthResultsView />
            </Border>

        </controls:UniformGrid>
    </StackPanel>
</UserControl>
