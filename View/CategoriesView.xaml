<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Haushaltsbuch.View.CategoriesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Haushaltsbuch.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <controls:DockPanel VerticalSpacing="10">
        <TextBlock controls:DockPanel.Dock="Top" Text="Ausgaben nach Kategorie" FontWeight="SemiBold" FontSize="16" />

        <ListView controls:DockPanel.Dock="Bottom" ItemsSource="{Binding CategorySummary}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:DockPanel>
                        <StackPanel controls:DockPanel.Dock="Left" HorizontalAlignment="Stretch">
                            <TextBlock Text="{Binding CategoryName}" />
                        </StackPanel>
                        <TextBlock controls:DockPanel.Dock="Right" HorizontalTextAlignment="Right" Text="{Binding FormattedTotal}" />
                    </controls:DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0 0 0 5" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </controls:DockPanel>
</UserControl>
