<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Haushaltsbuch.View.MonthResultsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Haushaltsbuch.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <StackPanel Spacing="10">

        <!-- Block Header -->
        <TextBlock Text="{Binding CurrentMonth}" FontWeight="SemiBold" FontSize="16" />

        <controls:UniformGrid Rows="2" Columns="2" ColumnSpacing="20" RowSpacing="15">
            <Border Grid.Row="0" Grid.Column="0" Background="#d5f4e6" Style="{StaticResource MonthResultBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Einkommen" Foreground="#27ae60" />
                    <TextBlock Text="{Binding MonthlyIncomes}" Foreground="#27ae60" FontSize="24" FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Row="0" Grid.Column="1" Background="#fadbd8" Style="{StaticResource MonthResultBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Ausgaben" Foreground="#e74c3c" />
                    <TextBlock Text="{Binding MonthlyExpenses}" Foreground="#e74c3c" FontSize="24" FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Grid.ColumnSpan="2" Background="#dae8fc" Style="{StaticResource MonthResultBorderStyle}">
                <controls:UniformGrid Columns="2">
                    <StackPanel>
                        <TextBlock Text="Ersparnis" Foreground="#3498d8" />
                        <TextBlock Text="{Binding MonthlyDifference}" Foreground="#3498d8" FontSize="24" FontWeight="SemiBold" />
                    </StackPanel>
                    <TextBlock Text="{Binding MonthlyDifferencePercentage}" Style="{StaticResource LabelStyle}" VerticalAlignment="Center" HorizontalTextAlignment="Right" FontSize="14" />
                </controls:UniformGrid>
            </Border>
        </controls:UniformGrid>

    </StackPanel>
</UserControl>
